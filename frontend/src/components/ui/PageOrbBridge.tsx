/** Large gold orb bridging page hero into the section below — one continuous circle. */
export function PageOrbBridge() {
  return <div className="page-orb page-orb--bridge" aria-hidden="true" />;
}
